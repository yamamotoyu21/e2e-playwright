import { test, expect, Page } from '@playwright/test';
import planPage from '../general/pages/plans.page'

test.describe('plans page test', () => {
    test.beforeEach(async function ({ page }) {
        const planPage = new planPage()
            


        }

    )
})